# Savage Worlds Charakter-Generator - Requirements
# Python 3.8+ erforderlich
#
# Installation: pip install -r requirements.txt
#
# LINUX: System-Abhängigkeiten müssen VOR der Installation vorhanden sein!
#   Debian/Ubuntu: sudo apt-get install python3-dev libcairo2-dev libffi-dev pkg-config
#   Fedora/RHEL:   sudo dnf install python3-devel cairo-devel gcc pkg-config
#   Arch Linux:    sudo pacman -S cairo pkgconf
# Siehe README.md für Details.

# =============================================================================
# KERN-DEPENDENCIES
# =============================================================================

# KivyMD 2.0.1 - Material Design UI Framework (Entwicklungsversion von GitHub)
# Auf Commit gepinnt: reproduzierbare Builds (Desktop == APK). Der Commit
# enthaelt den Android-Touch-Fix #1850 (MDScrollView/MDDialog-Crash mit
# TextFields). NEUERE Staende sind derzeit UNBRAUCHBAR: Der M3CommonRipple-
# Umbau (04/2026) crasht MDTabsItem mit "FBO Initialization failed"
# (Fbo mit Hoehe 0, verifiziert 2026-08 mit Master 57b1b8b0).
https://github.com/kivymd/KivyMD/archive/365aa9b96eee63e0e29c04de297dd222f478fce5.zip

# Kivy - Cross-Platform GUI Framework (kompatibel mit KivyMD 2.0.1+)
kivy>=2.3.1,<3.0

# materialyoucolor: bei diesem KivyMD-Stand auf 2.x bleiben (alte theming-API).
# >=3.0 ist erst noetig ab dem theming.py-Umbau (07/2026, kivymd/KivyMD#1877).
materialyoucolor>=2.0.7,<3.0

# =============================================================================
# FUNKTIONALE DEPENDENCIES
# =============================================================================

# ReportLab - PDF-Export für Charakterbögen
reportlab>=3.6.0

# Pillow - Bildverarbeitung (für UI-Icons und Assets)
Pillow>=8.0.0

# Python-dateutil - Erweiterte Datums- und Zeitfunktionen (für Datei-Timestamps)
python-dateutil>=2.8.0

# =============================================================================
# PLATFORM-SPEZIFISCHE DEPENDENCIES
# =============================================================================

# Windows-spezifische Dependencies (nur auf Windows)
pypiwin32>=223; sys_platform == "win32"

# =============================================================================
# INSTALLATION
# =============================================================================

# Einfache Installation:
# pip install -r requirements.txt

# Alternative: Stabile Version für Produktionsumgebung
# pip install kivymd==1.2.0 kivy>=2.1.0,<2.3.0 reportlab>=3.6.0 Pillow>=8.0.0 python-dateutil>=2.8.0

# Upgrade bestehender Installation:
# pip install --upgrade -r requirements.txt

# =============================================================================
# KOMPATIBILITÄT
# =============================================================================

# Python Version: 3.8+
# Betriebssysteme: Windows 10+, macOS 10.14+, Linux (Ubuntu 18.04+)
# RAM: Minimum 2GB, empfohlen 4GB+
# Speicher: ~100MB für Dependencies + ~50MB für Anwendung

# =============================================================================
# ALTERNATIVE KONFIGURATIONEN
# =============================================================================

# Für stabile Produktion (empfohlen für kritische Anwendungen):
# kivymd==1.2.0
# kivy>=2.1.0,<2.3.0
# reportlab>=3.6.0
# Pillow>=8.0.0
# python-dateutil>=2.8.0

# Für neueste Features (aktuell — gepinnter Commit, siehe oben):
# https://github.com/kivymd/KivyMD/archive/365aa9b96eee63e0e29c04de297dd222f478fce5.zip
# kivy>=2.3.1,<3.0
# reportlab>=3.6.0
# Pillow>=8.0.0
# python-dateutil>=2.8.0